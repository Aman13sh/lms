# Environment Configuration Example
# Copy this file to .env and update with your actual values

# Environment
NODE_ENV=production

# Database Configuration
DB_USER=lmsuser
DB_PASSWORD=your-secure-database-password
DB_NAME=lmsdb

# Redis Configuration
REDIS_PASSWORD=your-secure-redis-password

# JWT Configuration
JWT_SECRET=your-very-long-random-jwt-secret-key-change-this
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-very-long-random-refresh-secret-key-change-this
JWT_REFRESH_EXPIRES_IN=7d

# Encryption Configuration (for sensitive data)
# Generate 32-byte key: openssl rand -hex 32
ENCRYPTION_KEY=your-32-byte-encryption-key-here-change-this
# Generate 16-byte IV: openssl rand -hex 16
ENCRYPTION_IV=your-16-byte-iv-here-change-this

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@yourdomain.com

# AWS Configuration (for S3 file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=lms-documents

# Payment Gateway (Razorpay)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-secret

# Logging
LOG_LEVEL=info
LOG_DIR=/app/logs

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost,https://yourdomain.com

# Frontend Configuration
VITE_API_URL=http://localhost:5000

# Optional: Development Tools (uncomment to enable)
# COMPOSE_PROFILES=dev